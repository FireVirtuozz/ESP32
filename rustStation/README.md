# Rust station

This project aims to learn the Rust language, starting with following the [official doc](https://doc.rust-lang.org/book/title-page.html).

```bash
curl --proto '=https' --tlsv1.2 https://sh.rustup.rs -sSf | sh
rustc --version
rustup update
```