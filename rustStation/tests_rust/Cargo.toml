[package]
name = "tests_rust"
version = "0.1.0"
edition = "2024"

[dependencies]
criterion = { version = "0.8.2", features = ["html_reports"] }
rand = "0.10.2"
slotmap = "1.1.1"

[[bench]]
name = "benchmark_buffer_types"
harness = false
